---
-
  hosts: 
    all
  vars_files:
    - servicename.yml
  tasks: 
    - name: jinja services template
      win_service:
        msg: >
            The task has done 
             {% for displayname=={{servicenames[0]}}  -%}
                   state==stopped
             {% endfor -%}        
...