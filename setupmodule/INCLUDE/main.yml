---
-

Hosts: all
gather_facts: true

 task: webrole folder creation 
 -name: includetask 
  -include_tasks: webrole.yml
   when: ansible_env.subrole == web 
 
 -name: include task
  -include_tasks: winrole.yml
   when: ansible_env.mainrole == win


...
